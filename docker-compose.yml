services:
  api:
    build:
      context: .
      dockerfile: ./PolaperLinku.Api/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ~/.data/polaper-linku:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5000
    networks:
      - polaper-network
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: ./polaper-linku-web/Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api
    environment:
      - VITE_API_URL=http://localhost:5000
    networks:
      - polaper-network
    restart: unless-stopped

networks:
  polaper-network:
    driver: bridge
